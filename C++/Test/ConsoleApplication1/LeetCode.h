#include <algorithm>;
#include <cmath>
#include <queue>
#include <deque>
#include <set>
#include <vector>;
using namespace std;


struct ListNode {
    int val;
    ListNode *next;
    ListNode(int x) : val(x), next(NULL) {}
};
